name: RecZone
run-name: Setting up RecZone database
on: [push]
jobs:
    test-output:
        runs-on: ubuntu-latest
        steps:
            - name: Test Output
              run: echo "here is a test";
    connect-remote:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Show databases
              run: mysql -h localhost --port 3306 -uroot -pYBt8-f9Ol(!Ifg@z -e "SHOW DATABASES";
